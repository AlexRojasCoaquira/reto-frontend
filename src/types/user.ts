export interface User {
  name: string
  lastName: string
  birthDay: string
}

export interface FormInsurance {
  documentType: string
  documentNumber: string
  phoneNumber: string
  acceptPrivacyPolicy: boolean
  acceptTerms: boolean
}
